
import Navigation from "@/components/Navigation";
import ThemePageLoader from "@/components/ThemePageLoader";

const Showcase = () => {
  return (
    <div className="min-h-screen bg-gray-50">
      <Navigation />
      <ThemePageLoader pageName="Showcase" />
    </div>
  );
};

export default Showcase;
